pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    echo 'Hello, World!'
                }
            }
        }
    }
    post {
        success {
            echo 'Pipeline completed successfully.'
        }
        failure {
            echo 'Pipeline failed.'
        }
    }
}
